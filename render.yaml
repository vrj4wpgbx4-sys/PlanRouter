services:
  - type: web
    name: planrouter-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -k uvicorn.workers.UvicornWorker api.api_main:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11